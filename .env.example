# Optional RSS feeds. Comma-separated.
RSS_FEEDS=https://news.google.com/rss/search?q=BBCA+saham&hl=id&gl=ID&ceid=ID%3Aid

# Number of hours to keep when polling.
POLL_INTERVAL_SECONDS=300

# Optional: set to 1 to use a lighter fallback sentiment model if needed.
